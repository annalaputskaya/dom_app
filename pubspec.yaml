name: dom_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  cupertino_icons: ^1.0.8
  lottie: ^3.3.1
  firebase_core: ^4.5.0
  firebase_auth: ^6.2.0
  cloud_firestore: ^6.1.3
  url_launcher: ^6.3.0
  image_picker: ^1.2.2
  firebase_storage: ^13.3.0
  intl: ^0.20.2
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0 
   

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/animations/


flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/MasterOK.png" 
